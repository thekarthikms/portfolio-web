const fontFamily = {
  sans: ["Inter", "sans-serif"],
}

const colors = {
  backgroundColor: "var(--background)",
  textColor: "var(--text)",
  linkColor: "var(--link)",
}

const extendTheme = {
  fontFamily,
  colors,
}

export default extendTheme
